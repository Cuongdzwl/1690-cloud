{{! div.container>div.row>div.col*2 }}

<div class="container col-md-10 text-center mt-5 pt-3 mb-3">
  <div class="row">
    {{! LEFT COLUMN }}
    <div class="col shadow-2">
      <img
        class="image-fluid"
        src="{{product.image}}"
        alt="toy"
        width="350"
        height="400"
      />
    </div>

    {{! RIGHT COLUMN }}
    <div class="col">
      <table class="ms-4">
        <tr><th class="pb-0"><a
              class="text-muted fs-6 text-truncate"
              href="/products/category/{{product.category}}"
              style="max-width: 10px;"
            >{{product.category}}</a></th></tr>
        <tr>
          <th class="pb-4 pt-0 mt-0"><span
              class="fs-2 text-wrap"
            >{{product.name}}</span></th>
        </tr>
        <tr>
          <th><span class="fs-6 price">$ {{product.price}}</span></th>
        </tr>
        <tr>
          <td class="pt-3">
            <div class="row">
              <div class="col-2">
                <button type="button" class="btn btn-warning"><i
                    class="fa fa-shopping-cart"
                    aria-hidden="true"
                  ></i>
                  Buy</button>
              </div>
              <div class="col-2">
                <p class="text-muted text-center mt-2">Only
                  <b class="price">{{product.quantity}}</b>
                  left!</p>
              </div>
            </div>
          </td>
        </tr>
      </table>
    </div>
  </div>
  <hr />
  <div>
    <h3 class="text-start mt-4 mb-4">Description</h3>
        <p class="text-muted text-start" style="font-size: 12px; font-style: italic;">Updated At: {{dateFormat product.updatedAt "DD/MM/yyyy"}}</p
    <div class="mb-6">
        <p class="text-start">{{product.description}}</p>
    </div>
  </div>
  <hr>
</div>